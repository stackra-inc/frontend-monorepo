# Product Overview

`@stackra-inc/ts-events` — Laravel-style event dispatcher for TypeScript —
pub/sub with wildcard listeners, subscribers, decorators, multiple drivers
(memory, redis, null), and RxJS streaming.

## Package Exports

- `@stackra-inc/ts-events` — main entry point
