/**
 * Constants Barrel Export
 *
 * - {@link TICKET_SERVICE} — DI token for TicketService
 * - {@link TICKET_REPOSITORY} — DI token for TicketRepository
 * - {@link TICKET_CONFIG} — DI token for TicketModuleOptions
 *
 * @module constants
 */

export { TICKET_SERVICE, TICKET_REPOSITORY, TICKET_CONFIG } from './tokens.constant';
