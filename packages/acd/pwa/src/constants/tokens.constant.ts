/**
 * DI Tokens for @stackra/ts-pwa
 *
 * |--------------------------------------------------------------------------
 * | Injection tokens for the PwaModule DI integration.
 * |--------------------------------------------------------------------------
 * |
 * @module pwa/constants/tokens
 */

/** Injection token for the PwaModule configuration. */
export const PWA_CONFIG = Symbol.for('PWA_CONFIG');
