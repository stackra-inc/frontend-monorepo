/**
 * @fileoverview Default configuration values for pull-to-refresh.
 * @module pwa/pull-to-refresh/constants
 */

export const PULL_TO_REFRESH_DEFAULTS = {
  THRESHOLD: 80,
  MAX_PULL: 150,
  ENABLED: true,
} as const;
