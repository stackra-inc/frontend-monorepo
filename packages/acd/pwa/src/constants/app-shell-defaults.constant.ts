/**
 * @fileoverview Default configuration values for the app shell.
 * @module pwa/app-shell/constants
 */

export const APP_SHELL_DEFAULTS = {
  STATUS_BAR_STYLE: 'default' as const,
  SAFE_AREA_PADDING: true,
  PREVENT_OVERSCROLL: true,
} as const;
