/**
 * Utilities Barrel Export
 *
 * Helper functions for the cache package.
 *
 * - {@link defineConfig} — Type-safe configuration helper following the
 *   `defineConfig` pattern used by Vite, Vitest, and other modern tools.
 *
 * @module utils
 */

export { defineConfig } from './define-config.util';
