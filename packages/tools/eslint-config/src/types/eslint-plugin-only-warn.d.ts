/**
 * Type declarations for eslint-plugin-only-warn
 *
 * This plugin doesn't have official TypeScript types,
 * so we provide a minimal declaration to satisfy TypeScript.
 */

/* eslint-disable @typescript-eslint/no-explicit-any */
declare module 'eslint-plugin-only-warn' {
  const plugin: any;
  export default plugin;
}
