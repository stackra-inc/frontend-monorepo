/**
 * Enum definitions for the multi-tenancy package.
 *
 * @description
 * This module exports all enum definitions used throughout the package.
 * Enums are stored in separate files following the convention:
 * - One enum per file
 * - File name: {enum-name}.enum.ts
 * - Exported via barrel export
 *
 * @module enums
 * @public
 */

export { TenantMode } from './tenant-mode.enum';
export { ResolverPriority } from './resolver-priority.enum';
